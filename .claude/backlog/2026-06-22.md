---
repo: LucasSantana-Dev/hitgate
generated_by: /backlog
generated_at: 2026-06-22T04:42:43Z
backlog_run_id: 20260622-012642
sources:
  audit_deep: 4 parallel read-only agents (test/security/code-quality/docs-packaging) + direct inspection
  ecosystem_health: skipped (standalone repo)
  repo_state_snapshot: ~/.claude/state/snapshots/LucasSantana-Dev__hitgate/backlog-2026-06-22.json
---

# Backlog 2026-06-22 — LucasSantana-Dev/hitgate

25 findings, ROI-ranked. 0 open issues at run time -> all new (no dedup hits). No feature findings -> no specs.

## Phase 1: Critical & High (this week)

### T1: Install-error message + PyPI URLs still say 'evidence-first-rag' [cat:bug] [sev:high] [effort:xs] (ROI 4.0)
acceptance: |
  - config.py error says `pip install hitgate[hybrid]`
  - pyproject URLs resolve to .../hitgate
evidence:
  - ragcore/config.py:91 tells users `pip install evidence-first-rag[hybrid]` (wrong package)
  - pyproject.toml:17-18 Homepage/Repository URLs use old repo name
suggested_approach: |
  Replace the package name in the [hybrid]-missing ImportError and the [project.urls] entries with hitgate.

### T2: External retriever output not validated -> opaque crash on bad shape [cat:bug] [sev:high] [effort:s] (ROI 2.0)
acceptance: |
  - malformed external retriever -> clear error naming the retriever
  - test covers missing/wrong-type path
evidence:
  - hitgate/run.py:90 `any(e in r['path'] ...)` KeyErrors if a result lacks 'path'; TypeErrors if not str
  - line 93 results[0]['path'] same assumption; the 'point it at YOUR retriever' promise needs a clear contract error
suggested_approach: |
  Validate each result dict (has str 'path') and raise a clear ValueError naming the retriever on violation.

## Phase 2: Medium (this month)

### T3: README shows `pip install hitgate` but package not yet on PyPI [cat:docs] [sev:medium] [effort:xs] (ROI 2.0)
acceptance: |
  - README install path is accurate vs PyPI reality
evidence:
  - README.md:5-6 install commands
  - pyproject version 0.1.0 built in dist/ only; not published -> `pip install hitgate` 404s today
suggested_approach: |
  Resolve by publishing 0.1.0 OR add a 'not yet on PyPI - pip install -e .[hybrid]' note. Coupled to the publish decision.

### T4: Code cites ADR-0010/0011 that don't exist in docs/adr/ [cat:docs] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - every ADR number cited in code exists in docs/adr/
evidence:
  - ragcore/retrieval.py:56,262,273,290 cite 'ADR 0011' (selective code-scope rerank)
  - docs/adr/ contains only 0001-0009 - decision the code relies on was never recorded
suggested_approach: |
  Write ADR-0010/0011 capturing the selective-code-rerank + reranker-fallback decisions, or correct the cited numbers.

### T5: 107 eval-artifact JSON files clutter the hitgate/ package dir [cat:tech-debt] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - hitgate/ holds source + load-bearing baselines only
  - CI green
evidence:
  - 107 *.json in hitgate/ (abl-*, abl17/23-*, auto_rr_m*, bge-*, eval30/50*, expanded-101-*)
  - only baseline.json/baseline.auto-rerank.json + a few referenced by check.sh/CI; not shipped in wheel but pollute source
suggested_approach: |
  Relocate throwaway experiment outputs to evals/ or gitignore; keep only CI load-bearing baselines after verifying check.sh.

### T6: Silent `except: pass` swallows query-log and LLM-fallback errors [cat:bug] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - failures are logged, not silently dropped
  - behaviour still non-fatal
evidence:
  - ragcore/retrieval.py:358-359 except Exception: pass on QLOG write (fully silent)
  - hitgate/generate.py:234-236 except: pass on LLM call
suggested_approach: |
  Log to stderr (or capture in metadata) so disk-full/schema/auth failures are visible; keep graceful return.

### T7: Missing CONTEXT.md despite project domain-doc convention [cat:docs] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - CONTEXT.md exists at root, covers core terms, links ADRs
evidence:
  - project CLAUDE.md + docs/agents/domain.md mandate 'one CONTEXT.md + docs/adr/ at root'
  - file absent; improve-codebase-architecture/grill-with-docs rely on it
suggested_approach: |
  Author a concise CONTEXT.md: domain terms (retriever/harness/Hit@K/gate/baseline/drift), modules, entry points, link ADRs.

### T8: pyproject.toml missing PyPI classifiers (pre-publish polish) [cat:tech-debt] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - classifiers present and render on PyPI
evidence:
  - pyproject.toml has no [project] classifiers
  - PyPI recommends Dev Status/License/Python versions/Topic/Audience
suggested_approach: |
  Add classifiers before the first PyPI upload (Dev Status :: 4 - Beta, License, Python 3.10-3.12, Topic, Intended Audience).

### T9: Gate IO/formatting untested: hitgate/compare.py at 47% coverage [cat:test] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - print_table + main covered
  - verdict.json write asserted
evidence:
  - compare.py 47% (compare() core tested; print_table 75-120 + main 124-140 + verdict.json write untested)
suggested_approach: |
  Test print_table regression markers and main() verdict.json write + exit codes.

### T10: hitgate/run.py error paths + metrics under-tested (65%) [cat:test] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - load_retriever bad-spec cases tested
  - metrics n=0 / MISS tested
evidence:
  - run.py 65%; load_retriever error cases (empty/':'-only spec) + metrics() edge cases (n=0, hit_rank None) untested
suggested_approach: |
  Add tests for load_retriever errors and metrics math edge cases.

### T11: hitgate/diff.py has 0% test coverage [cat:test] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - each verdict path tested
  - rank_val(None) tested
evidence:
  - diff.py 0% (76 stmts); REGRESSION/IMPROVEMENT/MIXED verdict + rank_val(None) untested
suggested_approach: |
  Unit-test the three verdict classes and MISS rank handling.

### T12: ragcore/query.py CLI entry point has 0% coverage [cat:test] [sev:medium] [effort:s] (ROI 1.0)
acceptance: |
  - arg combos tested
  - no-results exit 1 tested
evidence:
  - query.py 0% (51 stmts); main() arg parsing (--scope/--rerank/--fast) + no-results exit untested
suggested_approach: |
  Add a CLI integration test for scope/rerank flags + JSON output + exit code.

### T14: ragcore/build.py indexing pipeline has 0% unit coverage [cat:test] [sev:medium] [effort:m] (ROI 0.5)
acceptance: |
  - classify + exclusion + batching unit-tested
evidence:
  - build.py 0% unit (210 stmts) - happy path IS exercised by the CI eval, but classify_repo/type, exclusion, _flush batching, prefix logic lack unit tests
suggested_approach: |
  Unit-test classify_*, is_excluded_path, _flush batching with a mock DB/embedder.

### T15: ragcore/pack.py context-packing logic untested (0%) [cat:test] [sev:medium] [effort:m] (ROI 0.5)
acceptance: |
  - budget cap + ordering + empty-results tested
evidence:
  - pack.py 0% (81 stmts); pack() budget burndown, per-chunk caps, ordering, empty-results fallback untested
suggested_approach: |
  Mock search() and test budget enforcement, ordering, and no-match fallback.

### T16: ragcore/retrieval.py auto-rerank decision paths under-tested (66%) [cat:test] [sev:medium] [effort:m] (ROI 0.5)
acceptance: |
  - auto-trigger conditions tested
  - code-scope filter tested
evidence:
  - retrieval.py 66%; auto-rerank trigger (threshold/margin, lines 257-275) + RAG_CODE_RERANK scope filter untested (reranker FALLBACK is well-handled)
suggested_approach: |
  Test auto-rerank fires on top1<threshold and margin<RERANK_AUTO_MARGIN; test code-scope filter.

### T17: hitgate/generate.py complex branches under-tested (77%) [cat:test] [sev:medium] [effort:m] (ROI 0.5)
acceptance: |
  - callback-gen path + token cap tested
evidence:
  - generate.py 77%; callback-handler generation (383-434) + template substitution edge cases untested
suggested_approach: |
  Test callback generation + variable substitution + MAX_TOKENS cap.

### T18: by_scope not gated in compare.py (only aggregate + by_intent) [cat:refactor] [sev:medium] [effort:m] (ROI 0.5)
acceptance: |
  - gate scope set is decided + documented
  - if added: a masked-at-aggregate regression test exists
evidence:
  - compare.py:27-49 gates aggregate metrics + by_intent hit@5; by_scope deltas never gated -> a scope-specific regression can hide if aggregate stays within +-5pp
  - cf ADR-0003 (per-intent gating deferred) - may be deliberate
suggested_approach: |
  DECIDE: add by_scope hit@5 gating mirroring by_intent, or document why it's deferred (noise). Possible over-engineering risk - confirm need first.

## Phase 3: Low (when convenient)

### T13: Document the --retriever dynamic-import trust boundary [cat:security] [sev:low] [effort:xs] (ROI 1.0)
acceptance: |
  - README documents the --retriever trust boundary
evidence:
  - hitgate/run.py:64 importlib.import_module on user-supplied --retriever spec executes arbitrary code (inherent to feature; dev/CI-controlled)
suggested_approach: |
  Add a short README security note: --retriever loads/executes Python; point it only at trusted, code-reviewed modules. (An allowlist would be over-engineering.)

### T19: Cosmetic stale 'evidence-first-rag' refs (docstrings/fixture/chart/CLAUDE.md) [cat:tech-debt] [sev:low] [effort:s] (ROI 0.5)
acceptance: |
  - no non-historical evidence-first-rag refs in code/tests/CLAUDE.md/chart titles
evidence:
  - ragcore/config.py:1, ragcore/__init__.py:1, adapters/{langchain,llamaindex,langfuse}*.py:1 docstrings
  - tests/test_mcp_server.py:34 fixture, CLAUDE.md:5 issue-tracker ref, hitgate/plot_history.py:131 chart title (user-visible)
suggested_approach: |
  Grep-replace remaining non-historical evidence-first-rag refs with hitgate (exclude docs/adr + METHODOLOGY historical, exclude on-disk absolute paths).

### T20: hitgate/test_determinism.py is a test module shipped in the package [cat:tech-debt] [sev:low] [effort:s] (ROI 0.5)
acceptance: |
  - test code not shipped to users OR documented intentional
  - determinism gate still runs in CI
evidence:
  - hitgate/test_determinism.py ships in the wheel; invoked via `python -m hitgate.test_determinism` in eval.yml:55 (CI), not pytest
suggested_approach: |
  Move to tests/ (run via pytest) OR keep as intentional runtime CLI but exclude from wheel + document.

### T21: Orphaned modules: audit_contamination.py / plot_history.py not wired as CLI [cat:tech-debt] [sev:low] [effort:s] (ROI 0.5)
acceptance: |
  - intent documented
  - exposed-or-relocated consistently
evidence:
  - both in hitgate/ but absent from [project.scripts]; ambiguous whether user-facing or maintainer-only
suggested_approach: |
  Either expose as hitgate-audit / hitgate-plot-history scripts, or move to a _internal/ subpackage; document intent.

### T22: Adapter to_harness pattern duplicated across 3 adapters [cat:tech-debt] [sev:low] [effort:s] (ROI 0.5)
acceptance: |
  - shared protocol documented + one adapter uses it (only if pursued)
evidence:
  - adapters/langchain_retriever.py, llamaindex_retriever.py + example share identical to_harness(...) signature
suggested_approach: |
  OPTIONAL: extract a shared protocol in adapters/__init__.py. Over-engineering risk for only 3 small adapters - defer unless a 4th lands.

### T23: hitgate/audit_contamination.py untested (0%) [cat:test] [sev:low] [effort:m] (ROI 0.25)
acceptance: |
  - three classify verdicts + exit codes tested
evidence:
  - audit_contamination.py 0% (88 stmts); load_cases/classify/main verdicts + exit codes untested (maintainer tool)
suggested_approach: |
  Unit-test classify() (ok/CONTAMINATED/scope-mismatch) + load_cases validation + exit codes.

### T24: Config: 17 RAG_* env vars, no fail-fast validation [cat:tech-debt] [sev:low] [effort:m] (ROI 0.25)
acceptance: |
  - invalid values fail fast at import (only if pursued)
evidence:
  - ragcore/config.py reads ~17 RAG_* vars; typos silently fall back to defaults (no schema)
suggested_approach: |
  OPTIONAL: add a dataclass validator (NOT Pydantic - keep zero-dep core) that range-checks + fails fast. Over-engineering risk - defer unless typos bite.

### T25: hitgate/plot_history.py untested (0%) [cat:test] [sev:low] [effort:l] (ROI 0.125)
acceptance: |
  - commits() parse + no-matplotlib fallback tested
evidence:
  - plot_history.py 0% (84 stmts); commits()/hit5_at() worktree+subprocess chain + matplotlib fallback untested (high mocking cost, low value)
suggested_approach: |
  Low priority: mock git/subprocess; test commits() parse + matplotlib ImportError fallback.

## Run summary
- Created: 25 issues (#16–#40)
- Skipped: 0 duplicates (0 open issues at run time → all new)
- Failed: 0 creations
- Comments left on existing: 0
- Board: https://github.com/users/LucasSantana-Dev/projects/4 (Active Backlog) — 25 cards added, Priority+Effort set, highest-ROI on top
- Specs: 0 (no feature findings → spec-gen skipped)
- Tiers: 2×high/P1, 15×medium/P2, 8×low/P3
